server:
  port: 8089

spring:
  datasource:
    url: jdbc:mariadb://localhost:3306/notification
    username: root
    password: lehre
  jpa:
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        jdbc:
          lob.non-contextual-creation: true
        show_sql: true

